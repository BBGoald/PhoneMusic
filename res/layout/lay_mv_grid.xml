<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/layout_background"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >

    <GridView
        android:id="@+id/gv_type"
        style="@style/gridview_style1"      
        android:numColumns="3"      
        android:paddingBottom="@dimen/_2px_in720p"
        />

    <GridView
        android:id="@+id/gv_mv"
        android:layout_below="@+id/gv_type"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
      
        android:gravity="center"
        android:horizontalSpacing="@dimen/_2px_in720p"
        android:listSelector="@drawable/item_selector"
        android:numColumns="@integer/grid_clums_no_refresh"
        android:scrollbars="none"
        android:verticalSpacing="@dimen/_24px_in720p" />

    <TextView
        android:id="@+id/msg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="gone" />

</RelativeLayout>